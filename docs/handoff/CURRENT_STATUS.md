# 当前项目状态

更新时间：2026-09-03

## 主线状态

有数字人口播一键审核预览 V1.4 已通过本机短样本：4 句双主持、Standard 24GB、精确帧音频到全片预览均到达 `completed/review_ready`；1080×1920/30FPS、约16.5秒、7/7 0失败。OOM仅重试失败角色，保留成功结果，<¥0.50，未用 Plus。

供应商原片仅在严格尾静音区少帧；输入签名、V2 清单、PTS、静音及语音终点均通过才做本地末帧规范化，不新增上传或付费提交。

无数字人 V1.3 已真实到达 `preview_ready`。批量补画面、逐句草案编辑、声音设置与失败槽幂等恢复均已接入一键路线。

横屏原视频主角窗 V1 已完成：`focus_card` 保持比例、分层、静音1倍速、帧对齐和安全区；预览/成片共用物化，25FPS 源先转项目 CFR。五个机械鸭短预览通过，未调用模型或付费服务。

本地素材编排 V1 只读取已完成且指纹匹配的视觉理解 V2 索引，先出草案、再由用户按场景采用；采用后保留明确入/出点与静音 1 倍速，禁止自动拆分、循环、下载或覆盖锁定画面。合入后的本地合成、时间线/组合、API/UI 聚焦回归已通过；私有素材仍须人工预览。

本地素材准备/批量理解：总览可多选上传，确认启动“批量理解本地视频”。服务端串行V2队列，单条失败不中断；恢复不重提完成子任务。工作台148项回归通过；4754重启后生效。

素材视觉理解 V2 已完成短样本验收：提供内容指纹、镜头级抽帧/去重、结构化描述和工作台证据查看；关键帧外发须确认，同配置可复用。自动选材和长视频规模验收尚未完成。

## 固定生产合同

- InfiniteTalk 精确帧 API 模板：`config/runninghub/workflow-2094449979141218305.api.json`。
- 默认机型为 Standard 24GB。Plus 不是全局默认；只有单次父任务已在唯一前置确认中明确授权、同一角色前两次均为明确终态 OOM 且预算再次通过时，才允许第 3 次使用一次 Plus。
- 未知 RunningHub 响应进入 `ambiguous` 并停止，禁止重复提交。
- `review_ready` 不是批准或发布；成片仍需人工观看。

## 当前阶段

GitHub 私库为 `https://github.com/hrbj18/Haike_video`，`main`/`dev`/`v0.1.1` 均为 `50352aaa`；`START_HERE.md` 是接手入口，私有数据未进入 Git。新电脑按 `docs/DEPLOYMENT_WINDOWS_ZH-CN.md` 安装依赖后仍须迁移本地配置并授权真实短样本验证。

## 最近验证

- 本机“音频测试”真实 API 已验收；受限终端启动的工作台可能无法访问文本中转站，须以正常网络权限重启 `scripts/start_backlot.ps1 -Restart -NoBrowser`。
- 有数字人预览支持本地/豆包音色和角色正面图冻结；豆包双角色 4 句已验收为 24kHz PCM16、角色长轨一次对齐 25FPS。精确帧路线提交前拦截超过 300 秒计划，300 秒内仅把旧 120 秒母版限额扩展至所需值加 1 秒容差，不裁剪或变速。
- “9月2号新闻”真实有数字人审核预览已完成：12 句、豆包公版女/男声、2 个 Standard 24GB RunningHub 任务均复用成功，实际 ¥1.991；45 个主体画面槽全部完成。预览为 1080×1920、30FPS、139.061 秒 H.264/AAC，任务 7/7、失败 0、停在 `review_ready`，未发布。
- “9月2号新闻”新版预览为 SDR BT.709：HLG/PQ 转 SDR；右上圆形人物、左上五组标题与已确认配乐均已入片，未新付费。
- “机械鸭测试视频”横屏单主持预览已到 `completed/review_ready`、未发布：1920×1080/30FPS/133.189 秒，四源形成五段主角窗。RunningHub Standard 合计 ¥1.8481，无 Plus/重复提交；25FPS 时间码末帧与 Remotion 空裁切已修复，59 项回归和代表帧正常。
- 配音中心保留本地四音色和内置豆包雅雅/檬檬，支持粘贴豆包 ID 新增本机自定义音色；云端音色独立倍率/试听冻结。1.7B CPU `float32` 崩溃，`bfloat16` 可载入但性能未验收。
- 一键启动、横屏主角窗和静态检查已通过；回归见 `docs/LANDSCAPE_HERO_V1_ACCEPTANCE_2026-09-02_ZH-CN.md`。

- `v0.1.1` 密钥配置、发布与交接专项及无付费套件回归已通过；候选无真实密钥、私有媒体或付费产物。
- 完成态幂等恢复没有新增 worker、operation 或费用。
- 素材视觉理解 V2：聚焦回归 `13 passed`，后端 `783 passed, 6 skipped`；真实 Luna 多图预检和 4 条短素材验收通过，关键区间覆盖率、必需事实命中率均为 1.0，无证据断言为 0.0。
- 广州 Linux 无数字人真实验收：“键盘排序的秘密”从建项、Luna 写稿到审核预览一次跑通；父任务 16/16、失败 0，最终 `preview_ready`。成片 1080×1920、30 FPS、127.5 秒，代表帧与尾帧正常；未自动批准或发布。
- Linux 的 0.6B CustomVoice 仍保留；短句与 16 句性能未验收。自动报告 ASR 复核与 `music_present` 口径仍待 QA，未影响已完成媒体。

## 下一步

1. 用遮挡、快动作、小主体长视频做规模验收；性能质量未知。
2. 自动选材仍须保留 V2 证据和人工采用门，不得直接写入正式成片。
3. 发布整理已就绪：白名单审计通过；回归后 `dev`→`main`，禁止脏树整推。
4. 私有素材与配置不得经 GitHub 同步；真实 Pexels/RunningHub 验证仍需单独授权和预算。
5. 项目库为暖纸张逐行清单，暖纸张默认、深色手选；左栏固定，片段六分区/预览回退/选中态完成；回归通过，无付费写入。
