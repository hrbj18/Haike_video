# 当前项目状态

更新时间：2026-09-09

## 主线状态

工作台顶栏桌面吸顶；≤760px普通流。

统一队列：SQLite单通道统一预览/Codex任务，支持优先级、FIFO、幂等和恢复；完整视频父任务仍串行。4754已安全重启。单条双主持视频内部改为两个 RunningHub Plus 48GB 子任务先落账后云端并行；真实付费并行待独立授权验收。

新闻草案可编辑 `story_id`/小标题并标记结尾；同组一致，结尾不进标题图层；当前 T011 已标记结尾。

“9月8日新闻”v002 待审：6 张图均为 HyperFrames 3 秒居中卡片，S-006 置于 T001 开头，其余为 11 条唯一 Pexels 竖屏视频；v001 保留，未发布。

安全重启入口核对4754健康后打开工作台。

通用多标题V1.2已完成：新闻层写入 `story_headline_overrides`，与普通层共用revision/CAS及FFmpeg合同；标题页复用字幕控件和Pillow换行/字号规则，保存仅重做当前片段。“9月7号新闻”保留revision 2/S01字号77及既有片段，未重跑付费任务。

Cybercab 两条复刻片到 `preview_ready`，未批准/发布。

`米哈游AI女友复刻视频1` v002 已到 `preview_ready`，98.5秒；两层标题、短句字幕、视觉与配音合同已过自动检查，未批准或发布。

有数字人一键审核预览 V1.8 以整数 PCM 样本和 25FPS 清单为切割主合同，Whisper 只作诊断；新任务默认 Plus 48GB，每角色一次，双角色任务号逐个原子落盘后云端并行。`exact_frame_clock` QA 阻断漂移；仅尾静音少帧且证据全过时才本地规范化，不重提。

无数字人 V1.3 已到 `preview_ready`；批量补画面、逐句编辑、声音与失败槽恢复已接入一键路线。

横屏 `focus_card`、本地素材编排与批量理解已接入；V2草案需人工采用且不改锁定画面，串行失败不中断。

视觉 V2 短样已验收，长视频待验。快速概览 V1 的联系表、PTS映射、一次确认和≤12帧精细化已回归；>60分钟须选章。

户外精剪V2已完成：本地VAD、分句/动作保护、画面活动门、时间映射、持久异步候选和批量恢复。4个截句边界已机器修正；两事件7个疑点均未过画面门，保守0删减，听辨和可删正例仍为 `partial`。

户外二次剪辑V2完成：第一次切片按事件区分唯一当前/历史版本，旧父二次方案不可审批；精彩前置默认移动且正文不重复，显式预告模式才允许重复；超过最长目标或非预期重复由内容门阻断入库。四阿姨确定性夹具135.750→52.616秒、同源重复0、外部调用0、机器QA通过，真实模型选择及完整观看/试听仍为 `partial`。

## 固定生产合同

- InfiniteTalk 精确帧 API 模板：`config/runninghub/workflow-2094449979141218305.api.json`。
- 新任务默认 Plus 48GB（`instanceType=plus`），每角色最多一次；双角色开始前须一次冻结5元预算。Plus 仍明确 OOM 时停止并留待工作流优化，不自动降级或重提。冻结的旧 Standard/Lite 任务仍按原合同恢复。
- 未知 RunningHub 响应进入 `ambiguous` 并停止，禁止重复提交。
- `review_ready` 不是批准或发布；成片需人工观看。

## 当前阶段

GitHub：`dev` 发布 `29d2ecfa` 候选，`main`/`v0.1.1` 为 `50352aaa`；私有数据不进 Git。新电脑仍须迁移配置和短样授权。

## 最近验证

- 户外V2独立验收：303.369秒原片、84句、2场候选；输出135.750/91.100秒，保护碰撞0，媒体/时长QA通过；外部调用0，未入库。
- Plus并行改造：聚焦218项、Backlot 1101项通过（2项跳过）；真实付费生成未执行。
- 户外二次剪辑V2独立验收：父候选135.750秒→52.616秒，1.25倍速、4.816秒精彩前置、同源重复0；H.264/AAC、唯一映射/字幕、缓存、内容门和上游哈希检查全过，未调用模型或自动入库。相关217项、Backlot1109项通过（2项跳过）。
- 浏览器已见停顿、筛选、逐项恢复、双切口对照和一次保存；真实听辨、长样和Linux未验。
- 多标题左右一致、Cybercab两版、本机双角色25FPS及广州Linux无数字人既有验收保持有效；均未替代最终人工批准/发布。
- 米哈游AI女友复刻预览 v002：H.264/AAC、1080x1920、30FPS、98.500秒；-14.4 LUFS、True Peak -2.0 dBFS。英文产品名断词已局部修正；文字图层/视觉时间线/配音与全片预览相关21项回归通过，仍待真人完整观看与版权确认。

## 下一步

1. 等待人工观看Cybercab v003与复刻视频2 v003；调整仅改活动修订，不自动批准或发布。
2. 人工完整观看 `米哈游AI女友复刻视频1` v002，重点确认默认配音语气、两层常驻标题、Pexels人物风格与“原视频记录的当时数据”表述；只在确认后再批准，当前不发布。
3. 生产任务结束后安全重启/刷新工作台，再从四阿姨当前V2第一次切片做真实模型首跑与真人完整观看；逐切口听看、1.1/1.25听感、第二主体、两条独立素材和Linux仍待验。实现与逐项结果见 `docs/SINGLE_DEVELOPMENT_GUIDE_INTERACTION_SECOND_PASS_V2_ZH-CN.md` 及 `docs/INTERACTION_SECOND_PASS_V2_ACCEPTANCE_2026-09-09_ZH-CN.md`。
4. `dev` 经独立副本及 Linux 预发布验收后才可合入 `main`；私有素材/配置不进 Git。
5. 新闻成片仍须人工审看；另用私有短视频验收豆包 ASR 时间戳。
6. 待独立授权后用最短双角色样本验收真实 Plus 并行，只到待审；不以 mock 回归替代付费验收。
