# 当前项目状态

更新时间：2026-09-09

## 主线状态

工作台顶栏桌面吸顶；≤760px普通流。

统一队列：SQLite单通道统一预览/Codex任务，支持优先级、FIFO、幂等和恢复；完整视频父任务仍串行。4754已安全重启。单条双主持视频内部改为两个 RunningHub Plus 48GB 子任务先落账后云端并行；2026-09-09 已由“9月9日新闻”完成真实双任务付费并行验收。

新闻草案可编辑 `story_id`/小标题并标记结尾；同组一致，结尾不进标题图层；当前 T011 已标记结尾。

“9月8日新闻”v002 待审：6 张图均为 HyperFrames 3 秒居中卡片，S-006 置于 T001 开头，其余为 11 条唯一 Pexels 竖屏视频；v001 保留，未发布。

“9月9日新闻”v003 待审：4 张用户配图均为 HyperFrames 5 秒居中卡片，合计 20 秒；其余时段由 10 条唯一 Pexels 视频覆盖。雅雅/檬檬分别使用 Plus 48GB 单次付费任务并行生成，实际消费 0.761/0.502 元；雅雅供应商原片尾部短 3 帧，经 PTS、冻结 PCM 静音尾部和 8ms 帧内尾音容差核验后本地补齐，未重提。1080x1920/H.264/AAC/30FPS，61.068 秒，-14.13 LUFS/True Peak -1.92 dBTP；未批准、未发布。

通用多标题V1.2已完成：新闻层写入 `story_headline_overrides`，与普通层共用revision/CAS及FFmpeg合同；标题页复用字幕控件和Pillow换行/字号规则，保存仅重做当前片段。“9月7号新闻”保留revision 2/S01字号77及既有片段，未重跑付费任务。

有数字人一键审核预览 V1.8 以整数 PCM 样本和 25FPS 清单为切割主合同，Whisper 只作诊断；新任务默认 Plus 48GB，每角色一次，双角色任务号逐个原子落盘后云端并行。`exact_frame_clock` QA 阻断漂移；仅尾静音少帧且证据全过时才本地规范化，不重提。

无数字人 V1.6：`source_audio_only` 支持显式逐条定时字幕，片段字幕层与全片 SRT 共用合同；旧项目未配置时仍使用字符权重回退。整体响度维持新项目默认 -10 LUFS、旧项目 -14 LUFS，并支持 -16 至 -8 LUFS 及 ±0.5 LU 微调。“余承东和雷军汽车搞笑复刻1”v006 待审，未发布。

横屏 `focus_card`、本地素材编排与批量理解已接入；V2草案需人工采用且不改锁定画面，串行失败不中断。

视觉 V2 短样已验收，长视频待验。快速概览 V1 的联系表、PTS映射、一次确认和≤12帧精细化已回归；>60分钟须选章。

户外精剪V2已完成：本地VAD、分句/动作保护、画面活动门、时间映射、持久异步候选和批量恢复。4个截句边界已机器修正；两事件7个疑点均未过画面门，保守0删减，听辨和可删正例仍为 `partial`。

户外二次剪辑V2完成：第一次切片按事件区分唯一当前/历史版本，旧父二次方案不可审批；精彩前置默认移动且正文不重复，显式预告模式才允许重复；超过最长目标或非预期重复由内容门阻断入库。四阿姨确定性夹具135.750→52.616秒、同源重复0、外部调用0、机器QA通过，真实模型选择及完整观看/试听仍为 `partial`。

## 固定生产合同

- InfiniteTalk 精确帧 API 模板：`config/runninghub/workflow-2094449979141218305.api.json`。
- 新任务默认 Plus 48GB（`instanceType=plus`），每角色最多一次；双角色开始前须一次冻结5元预算。Plus 仍明确 OOM 时停止并留待工作流优化，不自动降级或重提。冻结的旧 Standard/Lite 任务仍按原合同恢复。
- 未知 RunningHub 响应进入 `ambiguous` 并停止，禁止重复提交。
- `review_ready` 不是批准或发布；成片需人工观看。

## 当前阶段

GitHub：`dev` 发布 `29d2ecfa` 候选，`main`/`v0.1.1` 为 `50352aaa`；私有数据不进 Git。新电脑仍须迁移配置和短样授权。

## 最近验证

- Plus并行改造：聚焦218项、Backlot 1101项通过（2项跳过）；“9月9日新闻”已完成真实双任务单次付费并行验收。
- “9月9日新闻”真实 Plus 并行与尾帧恢复：RunningHub 任务 `2097539599856525313`/`2097539625815072770` 均单次结算；总消费1.263元。雅雅原片910→规范化913帧，原片PTS连续、最后语音后3648个PCM采样全零、provider raw SHA保留；定向回归50项通过。最终 v003 的4张HyperFrames卡片各5秒，非卡片时段Pexels视频全覆盖，机器全片审查通过，仍待真人完整观看。
- 原音轨字幕/响度：“余承东和雷军汽车搞笑复刻1”已按 S-001 烧录字幕 30FPS 切换帧保存 32 个显示区间，v006 为1080×1920/30FPS/H.264/AAC、52.900秒，-10.3 LUFS/True Peak -1.85 dBTP；v005/v006 解码音频 MD5 相同。工作台/API回归198项通过，真实页面显示 v6 已就绪；仍待真人完整观看与试听。

## 下一步

1. 人工完整观看“9月9日新闻”v003，重点确认四张卡片节奏、Pexels语义、末句0.12秒补帧观感和音乐比例；当前只到待审，不发布。
2. 生产空闲时从四阿姨当前V2第一次切片做真实模型首跑与真人完整观看；逐切口听看、倍速、第二主体、独立素材和Linux仍待验。
3. `dev` 经独立副本及 Linux 预发布验收后才可合入 `main`；私有素材/配置不进 Git。
