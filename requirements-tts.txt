# OpenMontage embedded local TTS runtime (installed into its own virtualenv).
# Qwen3-TTS is Apache-2.0 and downloads model weights on first use.
qwen-tts==0.1.1
fastapi>=0.110,<1
uvicorn>=0.29,<1
soundfile>=0.12,<1
numpy>=1.24,<3
python-dotenv>=1.0,<2

