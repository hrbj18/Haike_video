# Haike Video 内容项目模板

每一期视频都应复制 `project.yaml`，使用小写英文项目 ID，例如：

```text
003-tech-chat
004-product-demo
005-ai-news
```

项目源文件保存在 `content/episodes/<project-id>/`。Haike Video 运行时产生的
`projects/<project-id>/`、`renders/`、`cache/` 和 `temp/` 不作为源项目提交。

